services:
  - type: web
    name: preppers-gurukul-student-desk
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: GOOGLE_SHEET_CSV_URL
        sync: false
